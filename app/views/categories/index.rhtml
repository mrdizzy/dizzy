<h1><%= bigarrow %> Categories</h1>

	<!-- LIST CATEGORIES -->
	<div id="categories">	
	<% @categories.each do |category| %>
		<p><span id="<%= category.id %>"><%= link_to category.name, category_path(category.permalink) %> </span></p>
	<% end %>
	</div>