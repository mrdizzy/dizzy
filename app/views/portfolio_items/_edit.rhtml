<% fields_for "company[portfolio_items_attributes][#{edit.object_id}]", edit do |c| %>

	<%= c.file_field :image %>
	<%= c.hidden_field :id %>
	<%= c.collection_select :portfolio_type_id, PortfolioType.all, :id, :description %>
	
<% end %>