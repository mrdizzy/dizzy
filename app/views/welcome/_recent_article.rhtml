	<span class="posted_in"><%= solid_arrow %> <%= recent_article.main_category.name.upcase %></span>

	<h3><%= asterisk %> <%= link_to recent_article.title, content_path(recent_article.permalink) %></h3>
	
	<h4>POSTED <%= time_ago_in_words(recent_article.date).upcase %> AGO</h4>
	
	<p><%= recent_article.description %></p>
	