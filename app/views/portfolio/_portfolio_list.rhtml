<% @companies.each do |company| %>
				<p><%= link_to_remote company.name, :url =>{ :action => "show_ajax", :id => company.id } %></p>
			<% end %>
			<%= link_to_remote "Previous", :url => { :action => "portfolio_list", :page => @company_pages.current.previous} if @company_pages.current.previous %>
			<%= link_to_remote "Next", :url => { :action => "portfolio_list", :page => @company_pages.current.next} if @company_pages.current.next %>